//array from deye akta string ke benge convert kore array te convert kora 
/*var title = "Riajul islam";
var arr = Array.from(title)
console.log(arr)*/

let myName = "Riajul islam pavel"
let arr = Array.from(myName)
console.log(arr.length)